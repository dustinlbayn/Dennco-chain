# Deployment artifacts

Contract deployment scripts write network-specific deployment artifacts into this directory.

Example output after local deployment:

```text
local/IdentityRegistry.json
local/AssetRegistry.json
```

Each artifact includes the contract name, deployed address, network name, chain ID, ABI, and deployment timestamp.
